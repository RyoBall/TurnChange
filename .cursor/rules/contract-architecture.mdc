---
description: 契约式编程、修改分级响应与禁止事项
alwaysApply: true
---

# 架构与契约规则

## 核心原则

本项目遵循契约式编程。任何修改都不得破坏已有代码的对外或内部契约。

## 对外契约

- 所有 `public` 方法、接口方法、事件的签名（函数名、参数类型和数量、返回值类型）视为对外契约
- 修改对外契约前，必须先搜索整个项目，找到所有调用方
- 输出受影响调用方的完整列表，**等待用户确认后才能修改**
- 内部实现可以优化或重构，但必须保证输入输出行为不变

## 内部契约

- 每个私有函数的职责（见函数名和顶部注释）视为内部契约
- 修改私有函数逻辑时，必须先检查同类中哪些其他函数调用了它
- 如果修改会影响调用方，必须在修改说明中列出所有受影响的内部函数
- 内部函数的拆分、新增不影响其他函数时，可以自由进行

## 修改分级响应机制

动手前先判断风险等级，按对应流程执行：

### 低风险（直接执行，无需分析报告）

- 私有函数的内部实现优化，且函数名、参数、返回值、外部行为不变
- 新增私有函数，不影响任何已有函数
- 修复明确的逻辑错误（如计算符号写反），且修正后行为符合函数名所描述的职责
- 修改注释、日志输出格式等不影响逻辑的内容

### 中风险（列出内部影响，无需遍历外部）

- 修改私有函数的参数或返回值类型
- 修改私有函数的职责范围（如原本只处理 A，现在要同时处理 B）
- 重构多个私有函数之间的调用关系

**输出：**「以下内部函数受到影响：[列表]。是否继续？」

### 高风险（必须遍历所有调用方并等待确认）

- 修改任何 `public` 方法的签名（函数名、参数类型/数量、返回值类型）
- 修改任何接口（interface）的成员
- 修改任何事件的参数类型或事件名称
- 修改序列化字段（`[SerializeField]`）的字段名或类型
- 修改枚举值（enum）的名称

**输出：**「以下外部调用方受到影响：[完整列表]。是否继续？」

## 修改后验证

- 中/高风险修改完成后，输出修改前后的行为对比，证明对外契约未变

## 禁止事项

- 禁止静默修改任何 `public` 方法或接口方法的签名
- 禁止修改已有事件的事件参数类型
- 禁止在生命周期函数（Awake、Start、Update 等）中直接写业务逻辑
- 禁止在非工厂/管理器脚本中直接使用 `Instantiate` 或 `Destroy`
- 禁止使用 `FindObjectOfType` 或 `GameObject.Find` 获取外部引用
- 所有外部引用必须通过 `[SerializeField]` 声明，由用户在编辑器中拖拽绑定
